package dev.gustavopere.rpgskilltree.compendium.discovery;

/** Trusted semantic trigger types consumed by the server-authoritative discovery runtime. */
public enum DiscoveryTriggerType {
    OBSERVATION,
    INSPECTION,
    INTERACTION,
    DEFEAT,
    TAME,
    BREED,
    VARIANT,
    HARVEST,
    PLANT,
    BIOME_ENTRY,
    DIMENSION_ENTRY,
    STRUCTURE_ENTRY,
    PHOTO,
    EXTERNAL
}
