package dev.gustavopere.rpgskilltree.core;

/** Facts exposed to declarative quest/progression conditions. */
public enum QuestProgressionFact {
    LEVEL,
    MASTERY_XP,
    CLASS_UNLOCKED,
    SPECIALIZATION_UNLOCKED,
    PERK_RANK,
    ATTRIBUTE_RANK
}
