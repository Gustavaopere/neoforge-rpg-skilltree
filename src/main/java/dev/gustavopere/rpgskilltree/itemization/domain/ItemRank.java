package dev.gustavopere.rpgskilltree.itemization.domain;

/** Canonical RPG item rank. Rank never determines modifier-family counts. */
public enum ItemRank {
    COMMON,
    UNCOMMON,
    RARE,
    EPIC,
    LEGENDARY,
    MYTHIC,
    UNIQUE
}
