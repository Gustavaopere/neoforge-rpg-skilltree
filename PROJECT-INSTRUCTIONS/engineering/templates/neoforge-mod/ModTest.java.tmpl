package {{JAVA_PACKAGE}};

import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.assertEquals;

class {{MAIN_CLASS}}Test {
    @Test
    void exposesCanonicalModId() {
        assertEquals("{{MOD_ID}}", {{MAIN_CLASS}}.MOD_ID);
    }
}
