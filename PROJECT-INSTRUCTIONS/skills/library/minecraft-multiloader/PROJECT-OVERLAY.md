# Project overlay — minecraft-multiloader

## Routing status
**Disabled by default.** The project target is NeoForge-only unless an explicit deliverable requires a second loader.

## Overrides
- Do not introduce Architectury/Fabric source sets merely because Architectury/Fabric-named libraries exist in the modpack.
- Minecraft 1.21.11 examples are reference-only for this 1.21.1 target.
- Any multiloader conversion requires an explicit architecture decision and separate compatibility proof.

## Authority
`../../VERSION-AUTHORITY.md`, current repository architecture and explicit user requirements.
