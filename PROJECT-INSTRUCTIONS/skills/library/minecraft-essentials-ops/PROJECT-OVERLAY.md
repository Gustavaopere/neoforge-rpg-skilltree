# Project overlay — minecraft-essentials-ops

## Routing status
**Not in the default project path.** EssentialsX/Paper administration is not proven by the current NeoForge 1.21.1 modlist.

## Overrides
- Use only when the user explicitly targets an EssentialsX/Paper server and provides or confirms that environment.
- Do not apply EssentialsX permissions/economy/warp semantics to the NeoForge modpack.
- Do not treat absence from the modlist as proof about a separately managed proxy/plugin server; require explicit evidence.

## Authority
`../../VERSION-AUTHORITY.md` and the actual server/plugin inventory for the requested environment.
