# Universal Material Chemistry — Review Checkpoints

Review is required at these boundaries during implementation:

1. after canonical modlist audit is frozen;
2. after core taxonomy/catalog invariants are green;
3. after Destroy 0.4.1 API contract is frozen;
4. after each provider-family material batch;
5. after scientific/arcane representation coverage reaches zero unexplained gaps for that family;
6. after client renderers pass deterministic tests;
7. after tooltip/encyclopedia integration;
8. after drift detection is wired into CI;
9. before merge, with full design-to-implementation traceability;
10. after merge, with final main SHA/workflows confirmed.
